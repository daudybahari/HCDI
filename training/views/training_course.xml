<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- training.course list view -->
        <record id="training_course_view_list" model="ir.ui.view">
            <field name="name">training.course.view.list</field>
            <field name="model">training.course</field>
            <field name="arch" type="xml">
                <list>
                    <field name="name"/>
                    <field name="description"/>
                </list>
            </field>
        </record>

        <!-- training.course form view -->
        <record id="training_course_view_form" model="ir.ui.view">
            <field name="name">training.course.view.form</field>
            <field name="model">training.course</field>
            <field name="arch" type="xml">
                <form string="">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <field name="user_id"/>
                        </group>

                        <notebook>
                            <page name="sesi" string="Sesi Kursus">
                                <group>
                                    <field name="session_line" nolabel="1" colspan="2">
                                        <list string="Sesi" editable="bottom">
                                            <field name="name"/>
                                            <field name="start_date"/>
                                            <field name="duration"/>
                                            <field name="seats"/>
                                        </list>

                                        <form string="Sesi">
                                            <group>
                                                <field name="name"/>
                                                <field name="start_date"/>
                                                <field name="duration"/>
                                                <field name="seats"/>
                                            </group>
                                        </form>
                                    </field>
                                </group>
                            </page>

                            <page name="deskripsi" string="Keterangan">
                                <group>
                                    <field name="description"/>
                                </group>
                            </page>
                        </notebook>

                    </sheet>
                </form>
            </field>
        </record>

        <record id="training_course_action" model="ir.actions.act_window">
            <field name="name">Kursus</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">training.course</field>
            <field name="view_mode">list,form</field>

            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                </p>
                <p>
                </p>
            </field>
        </record>

       <!-- This Menu Item must have a parent and an action -->
       <menuitem id="training_course_menu_act" name="Training kursus" parent="training_menu" action="training_course_action" sequence="40"/>
    </data>
</odoo>